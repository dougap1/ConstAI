/**
 * Stub for delete-reflection analytics. Replace with fetch / queue.
 * Extend signature with goal/task ids when wiring the API.
 * @param {string} reason
 */
export function recordTaskDeleteReflection(reason) {
  return reason.length
}
