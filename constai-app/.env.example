# Copy to .env.local (gitignored) and add your key from Google AI Studio.
# Never commit real keys — keep them only in .env.local on your machine.
VITE_GEMINI_API_KEY=

# Optional override (default: gemini-1.5-flash)
# VITE_GEMINI_MODEL=gemini-2.0-flash
